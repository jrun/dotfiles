export RUBYLIB="$RUBYLIB:$HOME/work/rip/lib"
export PATH="$PATH:$HOME/work/rip/bin"
eval `rip-config`

# use .localrc for settings specific to one system
[[ -f ~/.localrc ]] && .  ~/.localrc

if [[ -s $HOME/.rvm/scripts/rvm ]] ; then source $HOME/.rvm/scripts/rvm ; fi
